分布式对象存储
    1、对象的多版本存储的必要性及需求
        版本是用数值表示还是随机字符串
            用数值表示更直观，但容易报错，用字符串表示不容易报错但不直观
            如果是数值表示怎么保证并发情况下更好的人机交互(这个地方会版本冲突)
            如果是字符串表示怎么保证树状结构展示
    2、数据去重
    3、数据冗余
    4、元数据存储
        大量小文件下怎么快速定位位置(超过1亿之后定位相当慢)
    5、自动扩缩容的数据平衡问题

分布式缓存
    1、代理模式与

polardb(newDB)






























